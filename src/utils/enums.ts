export enum Status {
    SUCCESS = "success",
    ERROR = "error",
    FAIL = "fail",
}

export enum EmitTypes {
    ALL_USER = "ALL_USER",
    USER_HISTORY = "USER_HISTORY",
    EDITING_ORDERS = "EDITING_ORDERS",
    ORDER_ID = "ORDER_ID",
    SEND_ORDER_ID = "SEND_ORDER_ID",
    REMOVE_EDITING_ORDER = "REMOVE_EDITING_ORDER",
}

export enum SocketServer {
    ON = "on",
    OFF = "off",
}
